{
  "name": "healthcare-assistant-backend",
  "version": "1.0.0",
  "description": "Backend for Healthcare Assistant using Express.js, MongoDB, Mongoose, JWT, bcrypt, Twilio, OpenAI, Google Calendar, Stripe, and Winston",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^latest",
    "mongoose": "^latest",
    "jsonwebtoken": "^latest",
    "bcrypt": "^latest",
    "twilio": "^latest",
    "openai": "^latest",
    "googleapis": "^latest",
    "stripe": "^latest",
    "winston": "^latest"
  },
  "devDependencies": {
    "jest": "^latest",
    "nodemon": "^latest"
  },
  "engines": {
    "node": ">=latest"
  }
}
